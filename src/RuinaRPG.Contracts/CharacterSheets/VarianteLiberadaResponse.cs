namespace RuinaRPG.Contracts.CharacterSheets;

/// <summary>Uma Variante que o GM nomeou (hoje só a solar de Alóra) e que a ficha pode oferecer.</summary>
public record VarianteLiberadaResponse(string Variante, string Rotulo);
