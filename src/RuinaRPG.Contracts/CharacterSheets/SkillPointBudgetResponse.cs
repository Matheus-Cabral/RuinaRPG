namespace RuinaRPG.Contracts.CharacterSheets;

/// <summary>Perícias 2.d: "ver Tabela de Níveis para os Pontos de Perícia concedidos por nível" — reference only, not enforced.</summary>
public record SkillPointBudgetResponse(int GastoTotal, int PontosDisponiveis);
